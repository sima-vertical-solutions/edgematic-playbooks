id: edgematic-ros2-portable-pipeline
name: Edgematic ROS 2 Portable Pipeline
version: 0.4.3
agents: [codex, claude]
description: Detailed workspace, cross-build, deploy, board-preflight, environment, and detached-launch mechanics used by edgematic-ros2-user-package for a ROS 2 Neat pipeline the user owns; this is a mechanics reference, not the initial task router.
compatibility:
  env_types: [host, sdk]
  min_cli_version: 2.1.0
