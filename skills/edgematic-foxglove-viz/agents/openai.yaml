interface:
  display_name: "Edgematic Foxglove Visualization (Flora)"
  short_description: "Visualize live pipeline output in the bundled Flora app over foxglove_bridge"
  default_prompt: "Use $edgematic-foxglove-viz to show a running pipeline's embedded live output; when the user asks to see or show it, open it automatically with the final edgematic-flora directive instead of requiring a click."
