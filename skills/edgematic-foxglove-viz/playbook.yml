id: edgematic-foxglove-viz
name: Edgematic Foxglove Visualization (Flora)
version: 0.1.2
agents: [codex, claude]
description: Visualize a running pipeline's live output in the bundled Flora (Foxglove-fork) app at /flora, connected to the DevKit's foxglove_bridge — the yolov8_seg topic contract, board-side prerequisites, automatic embedded opening, and optional quick actions.
compatibility:
  env_types: [host, sdk]
  min_cli_version: 2.1.0
