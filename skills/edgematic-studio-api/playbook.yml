id: edgematic-studio-api
name: Edgematic Studio API
version: 0.1.0
agents: [codex, claude]
description: HTTP and WebSocket API guide for Edgematic Studio — projects, graphs, builds, deployments, runs, agentic tool invocation, devices, models, and LLM settings.
compatibility:
  env_types: [host, sdk]
  min_cli_version: 2.1.0
